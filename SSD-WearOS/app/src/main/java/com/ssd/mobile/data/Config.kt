package com.ssd.mobile.data

object Config {
    // Cambia esto por la URL real de tu API en producción.
    // 10.0.2.2 es el alias que usa el emulador de Android para llegar
    // a "localhost" de tu máquina cuando corres `php artisan serve`.
    const val API_BASE_URL = "http://10.0.2.2:8000/api/"
}
